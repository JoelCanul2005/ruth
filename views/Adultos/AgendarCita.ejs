<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Cita</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #f4f7f6;
            font-family: Arial, sans-serif;
        }
        .container {
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 50px;
        }
        h1, h2 {
            color: #333;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-secondary {
            margin-top: 20px;
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
            border-color: #545b62;
        }
        .form-group label {
            font-weight: bold;
        }
        .form-control {
            border-radius: 8px;
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.075);
        }
        .left-panel, .right-panel {
            padding: 15px;
        }
        .right-panel {
            border-left: 2px solid #f0f0f0;
        }
        .icon {
            font-size: 24px;
            color: #007bff;
            margin-right: 10px;
        }
        .alert {
            display: none;
        }
        .form-group input[type="text"], 
        .form-group input[type="date"], 
        .form-group input[type="time"] {
            border: 1px solid #ced4da;
        }
    </style>
    <script src="js/Adultos/crearCita.js" defer></script>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-4 left-panel">
                <h1><i class="fas fa-calendar-alt icon"></i> Solicitar Cita</h1>
                <div class="form-group">
                    <h2 class="h4"><i class="fas fa-user-md icon"></i> Selecciona un Psicólogo</h2>
                    <select id="psicologos" class="form-control">
                        <option value="">Seleccionar psicólogo</option>
                        <!-- Opciones se cargarán dinámicamente -->
                    </select>
                </div>
            </div>

            <div class="col-md-8 right-panel">
                <h2 class="h4 mb-4"><i class="fas fa-info-circle icon"></i> Detalles de la Cita</h2>
                <form id="citaForm">
                    <div class="form-group">
                        <label for="motivo">Motivo:</label>
                        <input type="text" id="motivo" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="fecha">Fecha:</label>
                        <input type="date" id="fecha" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="hora">Hora:</label>
                        <input type="time" id="hora" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="claveAutorizacion">Clave de Autorización:</label>
                        <input type="text" id="claveAutorizacion" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Crear Cita</button>
                </form>
                <div id="resultado" class="alert alert-info mt-4"></div>
                <a href="/A" class="btn btn-secondary btn-block">Volver</a>
            </div>
        </div>
    </div>

    <script>document.addEventListener('DOMContentLoaded', function() {
        const form = document.getElementById('citaForm');
        const resultado = document.getElementById('resultado');
    
        form.addEventListener('submit', function(event) {
            event.preventDefault(); // Evita el envío del formulario para pruebas
    
            // Aquí iría la lógica para manejar el formulario, como la validación o el envío a un servidor
    
            // Mostrar mensaje de alerta (ejemplo)
            resultado.textContent = 'La cita ha sido creada exitosamente!';
            resultado.classList.add('alert-success');
            resultado.classList.remove('alert-info');
            resultado.style.display = 'block';
    
            // Opcional: Ocultar el mensaje después de algunos segundos
            setTimeout(() => {
                resultado.style.display = 'none';
            }, 5000);
        });
    });
    </script>
</body>
</html>
